// TODO: Express server para enLocal Facturación
// Monta: mod-config, mod-catalogs, mod-invoicing, mod-reports
// Addons: mod-inventory, mod-pos

export async function startServer(_config: {
  port: number
  dbName: string
  enabledModules: string[]
  multiuser: boolean
}): Promise<any> {
  // Implementación en Fase 10
  return {}
}
