// TODO: Express server para enLocal Nóminas
// Monta: mod-config, mod-catalogs, mod-payroll, mod-reports
// Addons: mod-invoicing

export async function startServer(_config: {
  port: number
  dbName: string
  enabledModules: string[]
  multiuser: boolean
}): Promise<any> {
  // Implementación en Fase 10
  return {}
}
