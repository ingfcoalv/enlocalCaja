// TODO: Entry point Electron para enLocal Servicios (multi-usuario con QR)

import config from '../app.config'

console.log(`[${config.appName}] Starting on port ${config.port} (multiuser: ${config.multiuser})...`)
