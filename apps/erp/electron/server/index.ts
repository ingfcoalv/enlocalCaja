// TODO: Express server para enLocal ERP
// Monta TODOS los módulos default + addon mod-appointments
// Escucha en 0.0.0.0 (multiuser)

export async function startServer(_config: {
  port: number
  dbName: string
  enabledModules: string[]
  multiuser: boolean
}): Promise<any> {
  // Implementación en Fase 10
  return {}
}
