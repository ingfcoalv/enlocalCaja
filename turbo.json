{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["build"]
    },
    "lint": {},
    "package": {
      "dependsOn": ["build", "test"],
      "outputs": ["release/**"]
    },
    "clean": {
      "cache": false
    }
  }
}
